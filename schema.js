3. Object Relational Mapping (ORM) is a technique used in creating a "bridge" between object-oriented programs and, in most cases, relational databases.

CREATE SCHEMA xyz;

SELECT * FROM sys.schemas

CREATE TABLE schemaname.tablename(values...);
